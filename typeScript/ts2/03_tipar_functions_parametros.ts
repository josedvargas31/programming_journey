// cuando una funcion no devuelve nada, devuelve void, es decir, no importa si espera algo, y si devuelve algo, no importa lo que sea
// (name: string): Indica que la función fn debe aceptar un único parámetro llamado name, el cual debe ser de tipo string.
// => string: Indica que la función fn debe devolver un valor de tipo string.
/* const sayHiFromFunction = (fn: (name: string)=> string) => {
    return fn('Miguel');
}

const sayHi =  ((name: string) => {
    console.log('Hola ', name);
    return name;
})
sayHiFromFunction(sayHi) */