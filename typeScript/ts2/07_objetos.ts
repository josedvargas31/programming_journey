
// objetos
/* let hero = {
    name: 'thor',
    age: 1500,
};
function createHero(name: string, age: number) {
    return {name, age}
} */

// no sabemos si es del mismo tipo, thor, con hero, asi que aqui entra una cosa llamada type Alias
/* const thor = createHero('Thor', 1500) */